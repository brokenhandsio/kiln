#extend("base"):
#export("content"):
#if(page.breadcrumb):
<nav class="kiln-breadcrumb" aria-label="Breadcrumb">
    <ol>
        #for(crumb in page.breadcrumb):
        <li>#if(crumb.url):<a href="#(crumb.url)">#(crumb.name)</a>#else:<span aria-current="page">#(crumb.name)</span>#endif</li>
        #endfor
    </ol>
</nav>
#endif
<article class="kiln-article">
    #unsafeHTML(page.content)
</article>
#endexport
#endextend
